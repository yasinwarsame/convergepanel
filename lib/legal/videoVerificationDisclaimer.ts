/**
 * Canonical short disclaimer for video verification (API JSON + UI).
 * Keep in sync wherever users see the same limitation statement.
 */
export const VIDEO_VERIFICATION_DISCLAIMER =
  "This is an AI-assisted video authenticity review. Results should inform human judgment, not replace it. Vision models can miss or misread cues; use outcomes as signals, not as final authority.";
